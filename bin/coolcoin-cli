#!/usr/bin/env bash
set -Eeuo pipefail

ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"

exec "$ROOT/bin/coolcoin-cli.real" \
    -datadir="$ROOT/data" \
    -conf="$ROOT/data/coolcoin.conf" \
    "$@"
